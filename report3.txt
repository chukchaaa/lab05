# Лабораторная работа 03
## Цель работы
Данная лабораторная работа посвещена изучению систем автоматизации сборки проекта на примере CMake
## Ход работы
### Применяем начальные команды
```bash
export GITHUB_USERNAME=chukchaaa
cd ${GITHUB_USERNAME}/workspace
pushd .
source scripts/activate
```
### 1. Клонируем lab02 как основу для lab03
```bash
git clone https://github.com/${GITHUB_USERNAME}/lab02.git projects/lab03
cd projects/lab03
```
### 2. Меняем remote привязку с lab02 на lab03
```bash
git remote remove origin
git remote add origin https://github.com/${GITHUB_USERNAME}/lab03.git
```
### 3. Ручная компиляция (без CMake)
Компилируем библиотеку print
```bash
g++ -std=c++11 -I./include -c sources/print.cpp
ls print.o
nm print.o | grep print
```
nm покажет искаженные имена С++, символы _Z5print... и т.д.

Создаём статическую библиотеку
```bash
ar rvs print.a print.o
file print.a
```
Компилируем example1
```bash
g++ -std=c++11 -I./include -c examples/example1.cpp
ls example1.o
g++ example1.o print.a -o example1
./example1 && echo
```
Вывод:
```bash 
hello
```
Компилируем example2
```bash
g++ -std=c++11 -I./include -c examples/example2.cpp
nm example2.o
g++ example2.o print.a -o example2
./example2
cat log.txt && echo
```
Вывод:
```bash
hello
```

Очистка
```bash
rm -rf example1.o example2.o print.o
rm -rf print.a
rm -rf example1 example2
rm -rf log.txt
```
### 4. Создание CMakeLists.txt (по частям)
Первая часть: минимальные требования и проект
```bash
cat > CMakeLists.txt <<EOF
cmake_minimum_required(VERSION 3.4)
project(print)
EOF
```
Вторая часть: стандарт C++11
```bash
cat >> CMakeLists.txt <<EOF
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
EOF
```
Третья часть: статическая библиотека
```bash
cat >> CMakeLists.txt <<EOF
add_library(print STATIC \${CMAKE_CURRENT_SOURCE_DIR}/sources/print.cpp)
EOF
```
Четвёртая часть: пути для заголовочных файлов
```bash
cat >> CMakeLists.txt <<EOF
include_directories(\${CMAKE_CURRENT_SOURCE_DIR}/include)
EOF
```
```Шаг 5. Первая сборка через CMake
bash
cmake -H. -B_build
cmake --build _build
```
CMake создал папку _build и собрал библиотеку libprint.a.
```bash
CMake Deprecation Warning at CMakeLists.txt:3 (cmake_minimum_required):
  Compatibility with CMake < 3.10 will be removed from a future version of
  CMake.

  Update the VERSION argument <min> value.  Or, use the <min>...<max> syntax
  to tell CMake that the project requires at least <min> but has been updated
  to work with policies introduced by <max> or earlier.


-- Configuring done (0.0s)
-- Generating done (0.0s)
-- Build files have been written to: /home/dima/chukchaaa/workspace/projects/lab03/_build
[ 33%] Built target print
[ 50%] Building CXX object CMakeFiles/example1.dir/examples/example1.cpp.o
[ 66%] Linking CXX executable example1
[ 66%] Built target example1
[ 83%] Building CXX object CMakeFiles/example2.dir/examples/example2.cpp.o
[100%] Linking CXX executable example2
[100%] Built target example2
```

### 6. Добавляем исполняемые файлы в CMakeLists.txt
```bash
cat >> CMakeLists.txt <<EOF

add_executable(example1 \${CMAKE_CURRENT_SOURCE_DIR}/examples/example1.cpp)
add_executable(example2 \${CMAKE_CURRENT_SOURCE_DIR}/examples/example2.cpp)
EOF

cat >> CMakeLists.txt <<EOF

target_link_libraries(example1 print)
target_link_libraries(example2 print)
EOF
```
### 7. Сборка всего проекта
```bash
cmake --build _build
Сборка отдельных целей (для проверки)
bash
cmake --build _build --target print
cmake --build _build --target example1
cmake --build _build --target example2
```
```bash
CMake Deprecation Warning at CMakeLists.txt:3 (cmake_minimum_required):
  Compatibility with CMake < 3.10 will be removed from a future version of
  CMake.

  Update the VERSION argument <min> value.  Or, use the <min>...<max> syntax
  to tell CMake that the project requires at least <min> but has been updated
  to work with policies introduced by <max> or earlier.


-- Configuring done (0.0s)
-- Generating done (0.0s)
-- Build files have been written to: /home/dima/chukchaaa/workspace/projects/lab03/_build
[ 33%] Built target print
[ 66%] Built target example1
[100%] Built target example2
```
### 8. Проверка результатов
```bash
ls -la _build/libprint.a
_build/example1 && echo
_build/example2
cat log.txt && echo
rm -rf log.txt
```
Вывод:
```bash
-rw-rw-r-- 1 dima dima 2110 мая 15 06:46 _build/libprint.a
hello
hello
```

### 9. Замена CMakeLists.txt на готовый из tp-labs
```bash
git clone https://github.com/tp-labs/lab03 tmp
mv -f tmp/CMakeLists.txt .
rm -rf tmp
cat CMakeLists.txt
```
```bash
Клонирование в «tmp»...
remote: Enumerating objects: 91, done.
remote: Counting objects: 100% (30/30), done.
remote: Compressing objects: 100% (9/9), done.
remote: Total 91 (delta 23), reused 21 (delta 21), pack-reused 61 (from 1)
Получение объектов: 100% (91/91), 1.02 МиБ | 1.04 МиБ/с, готово.
Определение изменений: 100% (41/41), готово.
cmake_minimum_required(VERSION 3.4)

set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

option(BUILD_EXAMPLES "Build examples" OFF)

project(print)

add_library(print STATIC ${CMAKE_CURRENT_SOURCE_DIR}/sources/print.cpp)

target_include_directories(print PUBLIC
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
  $<INSTALL_INTERFACE:include>
)

if(BUILD_EXAMPLES)
  file(GLOB EXAMPLE_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/examples/*.cpp")
  foreach(EXAMPLE_SOURCE ${EXAMPLE_SOURCES})
    get_filename_component(EXAMPLE_NAME ${EXAMPLE_SOURCE} NAME_WE)
    add_executable(${EXAMPLE_NAME} ${EXAMPLE_SOURCE})
    target_link_libraries(${EXAMPLE_NAME} print)
    install(TARGETS ${EXAMPLE_NAME}
      RUNTIME DESTINATION bin
    )
  endforeach(EXAMPLE_SOURCE ${EXAMPLE_SOURCES})
endif()

install(TARGETS print
    EXPORT print-config
    ARCHIVE DESTINATION lib
    LIBRARY DESTINATION lib
)

install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/include/ DESTINATION include)
install(EXPORT print-config DESTINATION cmake)
```
### 10. Установка проекта
```bash
cmake -H. -B_build -DCMAKE_INSTALL_PREFIX=_install
cmake --build _build --target install
tree _install
```
Появилась папка _install с структурой include/ и lib/.
```bash
install
├── cmake
│   ├── print-config.cmake
│   └── print-config-noconfig.cmake
├── include
│   └── print.hpp
└── lib
    └── libprint.a

4 directories, 4 files
```

### 11. Отправка на GitHub
```bash
git add CMakeLists.txt
git commit -m "added CMakeLists.txt"
git push origin master
```
